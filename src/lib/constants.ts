/** Public repo behind the site — linked from the navbar, the footer and the landing CTA. */
export const REPO_URL = 'https://github.com/Nexiq7/trakr-web';

/** The backend — also MIT, and what a self-hosted trakr actually runs on. */
export const API_REPO_URL = 'https://github.com/Nexiq7/trakr-api';
